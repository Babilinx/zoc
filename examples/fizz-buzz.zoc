"std" @import const std
std.debug.print const print

pub fn main void :: void {
    0 while dup 100 < do {
        dup 3 % over 5 % + 0= if {
            "fizzbuzz "
        } else dup 3 % 0= if {
            "fizz "
        } else dup 5 % 0= if {
            "buzz "
        } print drop
    }
}

